package nlu.fit.cinemabookingbackend.config;

import org.springframework.context.annotation.Configuration;

/*
 * Thư mục này chứa các class cấu hình của Spring Boot:
 * - SecurityConfig (cấu hình JWT / bảo mật)
 * - CorsConfig (cấu hình CORS)
 * - AppConfig (cấu hình bean)
 */
@Configuration
public class AppConfig {
}
